import { PhotoFormatterPipe } from './photo-formatter.pipe';

describe('PhotoFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotoFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
